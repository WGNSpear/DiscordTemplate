const Discord =require("discord.js");
const fs = require("fs");
//any requirements go here
module.exports.run = async (bot, message, args) => {

    //Command code goes here

}

module.exports.help = {
    name: "template" //Must rename this to name of the file
}